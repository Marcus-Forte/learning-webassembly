{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Serve web (uv http.server 8080)",
            "type": "shell",
            "command": "uv run python -m http.server 8080",
            "options": {
                "cwd": "${workspaceFolder}/web"
            },
            "isBackground": true,
            "problemMatcher": [],
            "presentation": {
                "reveal": "always",
                "panel": "dedicated",
                "focus": false
            }
        }
    ]
}
